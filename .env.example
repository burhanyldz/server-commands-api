PORT=5100
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=server_commands
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=10
BOOTSTRAP_TOKEN=replace-with-bootstrap-token
CORS_ORIGIN=http://localhost:5173

# WebAuthn / Passkeys
# WEBAUTHN_RP_ID must match the hostname users access the app on (no port, no protocol)
# In production change to your actual domain, e.g. app.example.com
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Server Commands
# WEBAUTHN_ORIGIN must match window.location.origin of the frontend page
# That is: scheme + hostname + port ONLY — never include a path
# e.g. if your app is at https://example.com/server-commands, origin is still https://example.com
WEBAUTHN_ORIGIN=http://localhost:5173
